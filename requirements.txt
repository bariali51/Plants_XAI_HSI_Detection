# ==================================
# Nabtati — Python Dependencies
# Pinned for reproducible builds
# ==================================

# Django Framework
django>=4.2,<5.0
djangorestframework>=3.14,<4.0
python-decouple>=3.8,<4.0
whitenoise>=6.5,<7.0
gunicorn>=21.2,<22.0

# Task Queue (optional — required for background jobs)
celery>=5.3,<6.0
django-celery-beat>=2.5,<3.0
redis>=5.0,<6.0

# Machine Learning
numpy>=1.24,<2.0
tensorflow>=2.13,<3.0
opencv-python-headless>=4.8,<5.0

# Image Processing
pillow>=10.0,<11.0

# Data & Visualization
pandas>=2.0,<3.0
scipy>=1.11,<2.0
scikit-learn>=1.3,<2.0
matplotlib>=3.7,<4.0
plotly>=5.15,<6.0

# AI APIs (OPTIONAL — only needed for AI Assistant chat feature)
# Core disease detection is fully offline and does not need these
# google-generativeai>=0.3,<1.0
# openai>=1.0,<2.0

# Storage (for cloud deployments)
django-storages>=1.14,<2.0
